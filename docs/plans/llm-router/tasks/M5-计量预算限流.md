# M5 · 计量 / 预算 / 限流

> **层级**：阶段必读 + 任务卡 · **预计 2 天**
> **每次动手前先读**：[`../00-必读.md`](../00-必读.md)（约 2.5k tok）
> **本阶段要带上的备查文件**：[`../ref/code/C6-计量写入.md`](../ref/code/C6-计量写入.md)、[`../ref/code/C5-认证与闸门.md`](../ref/code/C5-认证与闸门.md) §7.13、[`../ref/R4-数据模型.md`](../ref/R4-数据模型.md) §5.6 / §5.7、[`../ref/R5-对外契约.md`](../ref/R5-对外契约.md) §6.2
> **前置阶段**：M4（转发链路）
> **收工动作**：在 [`../01-进度.md`](../01-进度.md) 勾选本阶段任务，并回填实际耗时

---

## 本阶段目标

把逐调用记录落库，接上预算与限流两道闸门。

**限流直接复用 `packages/agent-runtime` 里已有的 `RedisRateLimiter`**——那个包是死代码，但这个类实现完整、多副本正确性已考虑过（见 R1 §2.5）。

## 本阶段的硬约束（既定决策，不要自行改动方向）

- **计量失败绝不阻塞或失败上层调用**（A5 明文要求）。`enqueue()` 是同步、无 await、不抛错的。
- **预算开关与限流开关互相独立**（A6 明文要求）。`enforce=false` 时"不卡业务但仍计量"。
- 写 `llm_calls` 与累加 `llm_budget_usage` 必须在**同一个事务**里，保证不重不漏。
- 归属（subject）来自**令牌**，不来自请求头——沙箱内 agent 有 bash，能篡改 header。

## 易踩坑

- DB 故障时**不要把失败的批次回队**，会在故障期无限重放放大压力。丢弃 + 计数 + 告警。
- 队列必须有界，满了丢最老的，绝不 OOM。
- Redis 不可用时限流应**降级为放行**并告警（可用性优先），不要拒绝所有请求。
- 429 必须带 `Retry-After`：限流场景取滑动窗口剩余时间，预算场景取到窗口边界的秒数。
- 预算是**软限额**（缓存 TTL 10s），高并发下可能超出少量——这是刻意取舍，**必须写进验收报告**。

## 阶段完成判定

- [ ] 停掉 DB，调用仍成功返回，`recorder.stats().dropped` 递增
- [ ] `enforce=false` 且超限 → 放行且 `llm_budget_usage` 持续累加
- [ ] `enforce=true` 且超限 → 429 + `Retry-After` + 含 used/limit 的信息
- [ ] 两开关四种组合各自独立生效
- [ ] 全绿门禁通过

---

## 任务卡

> 标 **⇉** 的任务可与同阶段前一任务并行。
> **每张卡的完成定义（DoD，无例外）**：① 代码 + 测试在同一个 commit ② `pnpm build && check && lint && test` 全绿 ③ 新增 API route 有 `route.test.ts`、新增 service/utils 有单测，覆盖正常 + 至少一个错误路径 ④ Sparring Review 通过（MUST-FIX 清零）。
> 完整门禁见 [`../00-必读.md`](../00-必读.md) §七。

#### T5.1 `CallRecorder` + `DrizzleCallStore`
- **产出**：`src/metering/{call-recorder.ts,drizzle-call-store.ts}`（见 C6 §7.11）
- **要点**：`insertBatchWithBudget` 在**一个事务**里 INSERT `llm_calls` + upsert 累加 `llm_budget_usage`
- **验收**：`enqueue` 同步不抛错；攒够 batchSize 自动 flush；store 抛错时不抛给调用方且 `dropped` 递增；队列满时丢最老；`drain()` 清空；事务原子性（PGlite）
- **依赖**：T1.1, T4.4

#### T5.2 `BudgetService` ⇉
- **产出**：`src/budget/{budget-service.ts,drizzle-budget-store.ts}`（见 C5 §7.13）
- **验收**：无预算配置 → 放行；`enforce=false` 且超限 → **放行且仍计量**（A6 明写）；`enforce=true` 且超限 → 拒绝 + `retryAfterSec`；作用域优先级 agent→project→user→global；缓存 TTL 内不重复查 DB
- **依赖**：T1.1

#### T5.3 限流接线 ⇉
- **产出**：`src/guard/rate-limit.ts` + `apps/llm-router/src/middleware/rate-limit.ts`
- **要点**：**直接复用** `RedisRateLimiter`（`packages/agent-runtime`，见 R1 §2.5）；`llm-router` 的 `package.json` 加 `"@open-rush/agent-runtime": "workspace:*"`；Redis 不可用时**降级为放行**并告警（可用性优先）
- **验收**：超限返回 429；`Retry-After` 头存在；开关关闭时不生效；Redis 挂掉时放行
- **依赖**：T4.2

#### T5.4 错误信封与 429 语义
- **产出**：`src/errors.ts`（D10 的映射表）
- **验收**：每个错误场景在两种协议面下的信封形状；429 一定带 `Retry-After`；**上游错误体不被包装**（回归测试）
- **依赖**：T4.3

---
