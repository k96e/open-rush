# 01 · 当前进度

> **本目录唯一的可写文件。** 其他文档都是只读参考。
> 每完成一张任务卡：把 `[ ]` 改成 `[x]`，填实际耗时，必要时在「决策变更」「阻塞」里留一行。
> 若要推翻某条 D 决策：先在「决策变更记录」写清楚，再改 `ref/R2` 与 `specs/llm-router.md`——**不要只改代码**。

**基线**：`e62f507c37603cb7df40d87b5b4ee94a285503c7`
**最近更新**：2026-09-09 · Claude Code
**当前所在阶段**：_M3（M0 / M1 / M2 已完成，M2 的 Sparring 待补）_

---

## 总览

| 阶段 | 内容 | 预计 | 实际 | 状态 |
|---|---|---|---|---|
| M0 | 基线与规格 | 1 d | 0.5 d | ✅ 完成 |
| M1 | 数据层 | 1.5 d | 0.4 d | ✅ 完成 |
| M2 | 密钥盲写 | 1.5 d | 0.4 d | ✅ 完成（Sparring 待补） |
| M3 | 目录与路由 | 2 d | | ⬜ |
| M4 | 网关核心 | 3 d | | ⬜ |
| M5 | 计量 / 预算 / 限流 | 2 d | | ⬜ |
| M6 | 接线 | 2 d | | ⬜ |
| M7 | 验收与交付 | 1.5 d | | ⬜ |
| | **合计** | **~15 d** | | |

状态图例：⬜ 未开始 · 🟡 进行中 · ✅ 完成 · 🔴 阻塞 · ⏭️ 已裁剪

---

## 任务清单

### M0 · 基线与规格 → [`tasks/M0-规格与契约.md`](tasks/M0-规格与契约.md)

- [x] **T0.1** 写 `specs/llm-router.md`（含与 `credential-proxy.md` 的关系）— 过 Sparring　`0.25 d`
- [x] **T0.2** `packages/contracts/src/v1/llm-router.ts` + `ServiceTokenScope` 加 `llm:read`/`llm:write`　`0.25 d`

### M1 · 数据层 → [`tasks/M1-数据层.md`](tasks/M1-数据层.md)

- [x] **T1.1** 7 张 `llm_*` 表 + migration `0012_llm_router.sql`（含 `llm_catalog_state` 种子）　`0.3 d`
- [x] **T1.2** `@open-rush/db` 新增 `createNotificationListener`　`0.1 d`
- [x] **Sparring** M1 代码审查：2 条 MUST-FIX + 7 条 SHOULD-FIX/NIT 已处理，1 条转为阻塞项（CI）　`0.1 d`

### M2 · 密钥盲写 → [`tasks/M2-密钥盲写.md`](tasks/M2-密钥盲写.md)

- [x] **T2.1** `packages/llm-router` 骨架 + `crypto/sealed-box.ts`（10 例单测，超出要求的 8 例）　`0.15 d`
- [x] **T2.2** `crypto/key-loader.ts`（8 例）+ `scripts/gen-router-keypair.ts`（5 例）+ `pnpm llm:keygen`　`0.1 d`
- [x] **T2.3** 控制台凭据 API（POST/GET/DELETE/rotate）+ `DrizzleCredentialStore` + `bumpCatalogVersion`　`0.15 d`
- [ ] **Sparring** M2 代码审查 —— **本次未执行**，见「阻塞与待定」2026-09-09 条

**阶段完成判定**（M2 阶段文件 §阶段完成判定）：

- [x] `sealed-box.test.ts` 10 例全绿（往返含 UTF-8 与 8KB / 错误私钥 / 逐段篡改 / keyId 不符 / alg 不符 / 两次密文不同 / 截断 + 空密文 / 指纹一致 / 信封不含明文 / 外部生成的密钥对）
- [x] `credentials/route.test.ts` 断言真实响应体序列化后不含 `sealedValue` / `sealed_value` / `"value"` / 明文子串；`helpers.test.ts` 另打 `credentialToV1()` 的投影边界（领域对象多长一个 `sealedValue` 也不会漏出去）
- [x] `grep -rn "openSealed\|LLM_ROUTER_PRIVATE" apps/web packages/control-plane --include=*.ts` 输出为空（连注释里都不写这两个标识符的字面量，否则 M7·T7.3 的审计脚本会被自己的说明文字绊倒）
- [x] 全绿门禁：`pnpm build` / `check` / `lint` / `test` 四步 37/37 task 通过（新增 85 例：llm-router 40、web 45）

### M3 · 目录与路由 → [`tasks/M3-目录与路由.md`](tasks/M3-目录与路由.md)

- [ ] **T3.1** `CatalogStore` + `DrizzleCatalogStore`
- [ ] **T3.2** `CatalogCache`（LISTEN/NOTIFY + 轮询兜底）+ `bumpCatalogVersion`
- [ ] **T3.3** `resolveRoute`
- [ ] **T3.4** providers / models 控制台 API

### M4 · 网关核心 → [`tasks/M4-网关核心.md`](tasks/M4-网关核心.md)

- [ ] **T4.1** `apps/llm-router` 骨架 + `/healthz` `/readyz` `HEAD /api/hello`
- [ ] **T4.2** 令牌认证（`router-token` + `TokenAuthenticator`）
- [ ] **T4.3** `forward` + `headers` + `model-rewrite`
- [ ] **T4.4** `sse-tee` + anthropic/openai usage 解析 + `cost.ts`
- [ ] **T4.5** `/v1/messages` + `/v1/messages/count_tokens` + `/v1/models`
- [ ] **T4.6** `/v1/chat/completions`
- [ ] **T4.7** 🟡 `translate` adapter（**Stretch，可裁剪**——裁剪后须在验收报告 F2 一栏如实说明）

### M5 · 计量 / 预算 / 限流 → [`tasks/M5-计量预算限流.md`](tasks/M5-计量预算限流.md)

- [ ] **T5.1** `CallRecorder` + `DrizzleCallStore`（批写单事务）
- [ ] **T5.2** `BudgetService`（observe / enforce 两档）
- [ ] **T5.3** 限流接线（复用 `RedisRateLimiter`）
- [ ] **T5.4** 错误信封与 429 语义

### M6 · 接线 → [`tasks/M6-接线.md`](tasks/M6-接线.md)

- [ ] **T6.1** `LlmAccessService`（签发 / 吊销 / 聚合）
- [ ] **T6.2** `RunOrchestrator` 接线 + 修 `modelId` 断链
- [ ] **T6.3** `agent-worker` 密钥边界（A11 关键 3 行 + 回归测试）
- [ ] **T6.4** `control-worker` 装配
- [ ] **T6.5** 日志清洗（`sanitize`）与 `AuditAction` 补值

### M7 · 验收与交付 → [`tasks/M7-验收与交付.md`](tasks/M7-验收与交付.md)

- [ ] **T7.1** A1–A11 验证套件 + `docs/llm-router-acceptance.md`
- [ ] **T7.2** 性能基线脚本 + 结果
- [ ] **T7.3** 安全审计脚本 `scripts/audit-no-plaintext-key.sh`
- [ ] **T7.4** 文档（`docs/llm-router.md` / quickstart / roadmap / AGENTS.md）

---

## 验收指标状态

方法与命令见 [`ref/R6-验收自证.md`](ref/R6-验收自证.md)。**取舍一栏不能空着**——如实写取舍比全绿更有说服力。

| 指标 | 内容 | 状态 | 结果 / 取舍 |
|---|---|---|---|
| A1 | 透传语义（body 零改写 + SSE 不丢不改序） | ⬜ | |
| A2 | 转发性能（p95 附加延迟，TTFB 与总耗时分开报） | ⬜ | |
| A3 | 可用性（滚动更新不中断） | ⬜ | |
| A4 | 路由准确率（100% / 未知 404） | ⬜ | |
| A5 | 明细计量（逐调用 + subject + 与 run 级对账） | ⬜ | |
| A6 | 预算 / 限流（两开关独立） | ⬜ | |
| A7 | 目录热变更（NOTIFY + 轮询兜底两组数据） | ⬜ | |
| A8 | 密钥热变更（不重启生效 + 旧密钥不可还原） | 🟡 | M2 已落「旧密钥不可还原」的一半：rotate 覆盖 `sealed_value` 且不留历史密文，单测断言轮换后库里只解得出新密钥。「不重启生效」依赖 M3 的 `CatalogCache` |
| A9 | 安全审计（6 处探针 clean + 吊销流程） | ⬜ | |
| A10 | 失败隔离（502 且不泄露） | ⬜ | |
| A11 | 盲写 + 唯一持有（含堆快照对照实验） | 🟡 | M2 已落密码学前提（X25519 sealed box，web 只有公钥）+ 两道机械探针（grep 无解封调用点 / 响应体无密文）。堆快照对照实验仍归 M7 |

---

## 决策变更记录

推翻或收窄任何一条 D1–D12 时，在这里记一行，格式：`日期 · 编号 · 原结论 → 新结论 · 理由 · 已同步更新的文件`。

| 日期 | 编号 | 变更 | 理由 | 已同步 |
|---|---|---|---|---|
| 2026-09-08 | M1 Sparring 追加的 DB 侧约束（非 D 决策） | 在 R4 之外补 6 条约束与 2 个索引：`llm_catalog_state_singleton`（`id = 1`）、`llm_budgets` 的 subject_type / window 枚举、`llm_budget_usage` 的 subject_type 枚举、`llm_credentials` 的 auth_style 枚举与「authStyle='header' 必须带 authHeader」、`llm_budget_usage` 增代理主键 `id`、`llm_calls_token_idx` 与 `llm_router_tokens_project_idx` | ① 主键挡不住第二行（实测 `INSERT … (2, 5)` 通过），而 `bumpCatalogVersion` 只写 `WHERE id = 1`，多出来的行会让 M3 轮询读到永远没人 bump 的版本位；② llm_budgets 与 llm_budget_usage 之间没有外键、只靠约定对齐，一个大小写差异就生出一行永不触限也永不报错的影子累计器；③ 无主键表在逻辑复制下连 UPDATE 都做不了，而这是一张只有 UPDATE 的表；④ 删 run 会级联删 token，PG 随后要在 llm_calls（全库最大表）上全表扫描把 token_id 置 NULL | `0012_llm_router.sql`、对应 schema 文件注释、`llm-router-schema.test.ts` 与 `migration.test.ts` 的断言 |
| 2026-09-08 | R4 §5.7（非 D 决策） | `llm_budget_usage` 的复合主键 → **`UNIQUE NULLS NOT DISTINCT (subject_type, subject_id, window_key)`**；`llm_budgets` 的唯一约束同样加 `NULLS NOT DISTINCT` | PostgreSQL 的 PRIMARY KEY 隐含 NOT NULL，而 `subject_type='global'` 的行 `subject_id` 必须为 NULL（与 `llm_budgets`、v1 契约同一套语义）。且默认的 NULL-distinct 语义下，全局行既能重复插入、`ON CONFLICT` 也推断不到目标——累加会静默变成插入。唯一约束同样可作 ON CONFLICT 推断目标，累加语义不变。最低 PG 版本 15（仓库 docker-compose 为 pg16、测试用 PGlite 0.4/PG17，均满足） | `packages/db/src/schema/llm-budgets.ts` 的表注释、`0012_llm_router.sql`、`migration.test.ts` 的行为断言 |
| 2026-09-09 | M2 实施位置（非 D 决策） | ① `bumpCatalogVersion` 从 T3.2 **提前**落到 `packages/llm-router/src/catalog/bump-version.ts`；② 凭据数据访问放 `packages/llm-router/src/store/credential-store.ts`（`DrizzleCredentialStore`），路由只做鉴权 + 投影；③ `apps/web` 新增 `@open-rush/llm-router` 依赖 | ① T2.3 的实现要点明写「写成功后调 `bumpCatalogVersion(db)`」，不提前就没法满足；代码位置与签名与 `ref/code/C3` 一字不差，T3.2 直接在其上建 `CatalogCache` 即可。② 路由层直接拼 drizzle 会让「409 被引用」「keyset 分页」这类逻辑只能靠 mock 链断言；放进包里可以用 PGlite 真跑，同时让路由退化成一层薄投影——这正是盲写不变量 3 想要的形状。③ 是 ②+seal 的必然结果 | `packages/llm-router/**`、`apps/web/app/api/v1/llm/credentials/**`、`apps/web/package.json` |

---

## 阻塞与待定

| 日期 | 事项 | 影响的任务 | 状态 |
|---|---|---|---|
| | T6.1 的依赖方向：control-plane 依赖 llm-router，还是把 `mintRouterToken`/`hashRouterToken` 下沉到 contracts？ | T6.1 | 待 Sparring 定夺 |
| 2026-09-08 | `pnpm build` 在干净容器里会红：`apps/web` 需要 `DATABASE_URL` / `AUTH_SECRET`，而 `turbo.json` 的 `build` task 未声明 env passthrough，Next 只能从 `apps/web/.env.local` 读。开发机有该文件所以看不出来；CI/新容器需要先建。 | 全阶段门禁 | 已知，未改 turbo.json |
| 2026-09-08 | CI 缺两道门（M1 Sparring 提出）：`.github/workflows/ci.yml` 只跑 `pnpm test`，既没有 `pnpm lint`，也没有 `specs/migration-policy.md` §CI Gate 写明的「干净 pg16 上重放 migration」。所以 M1 的 `test:integration` 只在本地 PG16 上验过，PGlite 是 PG17、证明不了部署目标。修法：给 ci.yml 加 `services: postgres: pgvector/pgvector:pg16` + 一步 `test:integration`，并补 `pnpm lint`。**本次未改** ci.yml——它不在 M1 的文件清单里，且加错会让仓库每个 PR 变红，留给 M7·T7.1 或单独一个 PR。 | M7·T7.1 | 待办 |
| 2026-09-08 | M2·T2.3 的硬要求（由 M0 Sparring 提出）：凭据路由必须走显式投影 `credentialToV1()`，并在 `route.test.ts` 断言真实响应体不含 `sealedValue` / `value`——`v1Success<T>` 是无约束泛型，契约 schema 不会自动作用于响应体。 | T2.3 | ✅ 已落实（`helpers.ts#credentialToV1` + 三个 `route.test.ts` 的 `raw` 断言） |
| 2026-09-09 | **M2 的 Sparring Review 未执行**。本次开发在远程容器里进行，既没有 `agent` CLI（`gpt-5.3-codex-xhigh`）也没有 `workflow review-code`，`/sparring:*` 命令不存在，跨 Agent 交叉审查无法发起。已用一轮自审代替（并据此补了 `helpers.test.ts`、把注释里的 `openSealed` / `LLM_ROUTER_PRIVATE` 字面量摘掉、把 `resolveRouterPublicKey` 的入参从 env 对象改成公钥字符串），但**自审不等于 Sparring**。下次在有 sparring 工具的机器上开工时，第一件事是补 M2 的 diff review，MUST-FIX 清零后再把上面那一行勾上。 | M2 全部 · M3 开工前 | 待办 |
| 2026-09-09 | `apps/web` 会把 `@open-rush/llm-router` 的整个 index 打进 bundle，其中包含解封函数——**没有调用点**（grep 探针为空），也**没有私钥**，所以 A11 的密码学前提不受影响，但「web 里连那段代码都不该有」这条更强的说法目前不成立。可选的收紧办法：M4 建出 `apps/llm-router` 之后，把 store/catalog 挪进服务端，web 只留一个 `@open-rush/llm-router/sealing` 子路径导出（只含 `seal` / `computeKeyId`）。现在做的话两个入口都会被 web 引到，等于没收紧。 | M4 / M7·T7.3 | 待评估 |
| 2026-09-09 | `apps/web` 的 OpenAPI 文档（`docs/specs/openapi-v0.1.yaml`）尚未收录 `/api/v1/llm/*`。`pnpm validate:openapi` 目前是绿的（校验器不强制新端点），所以不阻塞；按计划归 M7·T7.4。 | M7·T7.4 | 待办 |

---

## 环境准备

- [x] `pnpm install` 通过（顺带修掉 lockfile 里 `tsup@8.5.1` 的 peer 解析漂移，否则 `--frozen-lockfile` 直接失败）
- [x] `pnpm build && pnpm check && pnpm lint && pnpm test` 全绿（`apps/web/.env.local` 需先建，见上方 2026-09-08 的 build 阻塞项——该文件被 .gitignore，未入库）
- [ ] `./verify.sh` 基线全绿
- [ ] `pnpm db:up && pnpm db:push`
- [ ] 已跑 [`ref/R0-基线与复现.md`](ref/R0-基线与复现.md) §1.3 的差异复核，8 条现状事实仍成立
- [x] 生成密钥对的工具已就位：`pnpm llm:keygen`（输出两段 PEM + keyId + 两边各自的 env 写法）。**实际密钥仍需部署方自行生成并分发**——公钥进 web、私钥进 llm-router，两边不要都配
- [ ] 已建 `e2e/llm-router/fake-upstream.mjs`（A1/A2/A10 都依赖它）
- [ ] 已确认 Sparring Review 的执行方式（`AGENTS.md` §Sparring Review 执行方式）
