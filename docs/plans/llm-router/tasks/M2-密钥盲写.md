# M2 · 密钥盲写

> **层级**：阶段必读 + 任务卡 · **预计 1.5 天**
> **每次动手前先读**：[`../00-必读.md`](../00-必读.md)（约 2.5k tok）
> **本阶段要带上的备查文件**：[`../ref/code/C1-密钥盲写.md`](../ref/code/C1-密钥盲写.md)（**必带**，含已实跑验证的完整实现）、[`../ref/R5-对外契约.md`](../ref/R5-对外契约.md) §6.3 / §6.4
> **前置阶段**：M0（契约）、M1（`llm_credentials` 表）
> **收工动作**：在 [`../01-进度.md`](../01-进度.md) 勾选本阶段任务，并回填实际耗时

---

## 本阶段目标

打通「录入即盲写」闭环：apps/web 用**公钥**封装供应商密钥，llm-router 用**私钥**解封。web 侧物理上不具备解密能力。

这是 A11 的实现阶段，也是整个方案说服力最强的一环——**不是"我们约定不解"，而是"没有私钥就解不了"**。

## 本阶段的硬约束（既定决策，不要自行改动方向）

- **apps/web 与 packages/control-plane 里永远不出现** `openSealed` 调用、也不出现 `LLM_ROUTER_PRIVATE_KEY*` 环境变量。这条是可以用 `grep` 机械验证的（见 R6 的 A11 ②）。
- `llm_credentials` 表**没有明文列**，也不保留历史密文（轮换 = 覆盖 + `version++`）。
- 控制台凭据 API 是**平台级资源**：只接受 session 认证，**拒绝 service token**（照抄 `apps/web/app/api/v1/vaults/entries/route.ts` 对 `scope=platform` 的处理）。

## 易踩坑

- `seal()` 之后**立刻断开对明文的引用**，不要塞进任何日志、错误信息或响应。
- 解封失败（keyId 不匹配 / 密文损坏）的错误信息里**不能回显任何密文片段**——只说 credential 的 name。
- `LLM_ROUTER_PUBLIC_KEY` 缺失是**服务端配置错误**，返回 `INTERNAL` + hint，不是 `VALIDATION_ERROR`。照抄 `vaults/entries/helpers.ts` 的 `resolveVault()` 模式。
- 生成密钥对后，**公钥进 web 的配置、私钥进 llm-router 的配置**，两边不要都配。

## 阶段完成判定

- [ ] `sealed-box.test.ts` 8 例全绿（往返 / 错误私钥 / 篡改 / keyId 不符 / alg 不符 / 两次密文不同 / 截断 / 指纹一致）
- [ ] `credentials/route.test.ts` 断言响应 JSON 序列化后**不含明文子串**
- [ ] `grep -rn "openSealed\|LLM_ROUTER_PRIVATE" apps/web packages/control-plane --include=*.ts | grep -v __tests__` 输出为空
- [ ] 全绿门禁通过

---

## 任务卡

> 标 **⇉** 的任务可与同阶段前一任务并行。
> **每张卡的完成定义（DoD，无例外）**：① 代码 + 测试在同一个 commit ② `pnpm build && check && lint && test` 全绿 ③ 新增 API route 有 `route.test.ts`、新增 service/utils 有单测，覆盖正常 + 至少一个错误路径 ④ Sparring Review 通过（MUST-FIX 清零）。
> 完整门禁见 [`../00-必读.md`](../00-必读.md) §七。

#### T2.1 `sealed-box.ts`
- **产出**：`packages/llm-router/` package 骨架（`package.json` / `tsconfig.json` / `vitest.config.ts`，照抄 `packages/control-plane` 的形状）+ `src/crypto/sealed-box.ts`（代码见 C1 §7.1）
- **验收**：`__tests__/sealed-box.test.ts` 至少 8 例：
  1. seal → open 往返一致（含 UTF-8 与 8KB 长 key）
  2. 用**另一对**私钥 open → 抛错
  3. 篡改密文任一字节 → 抛错（GCM tag）
  4. 篡改 `keyId` → 抛"keyId mismatch"
  5. `alg` 不匹配 → 抛错
  6. 同一明文 seal 两次 → 密文不同（临时密钥随机性）
  7. 截断密文（< 60 字节）→ 抛"malformed"
  8. `generateRouterKeyPair()` 产出的 keyId 与 `computeKeyId(pub)` 一致
- **依赖**：无

#### T2.2 `key-loader.ts` + 生成 CLI ⇉
- **产出**：`src/crypto/key-loader.ts`（见 C1 §7.2）；`scripts/gen-router-keypair.ts`（输出两段 PEM + keyId + 一段"公钥给 web / 私钥给 router"的使用提示）
- **验收**：单测覆盖 FILE 优先、inline base64、inline PEM、都缺失抛错、非 X25519 密钥抛错
- **依赖**：T2.1

#### T2.3 控制台凭据 API
- **产出**：`apps/web/app/api/v1/llm/credentials/route.ts`（POST/GET）、`[id]/route.ts`（DELETE）、`[id]/rotate/route.ts`
- **实现要点**：
  - POST 里 `seal(process.env.LLM_ROUTER_PUBLIC_KEY!, body.value)`，**随后不再引用 `body.value`**
  - `LLM_ROUTER_PUBLIC_KEY` 缺失 → `v1Error('INTERNAL', ...)` 附 hint（照抄 `vaults/entries/helpers.ts` 的 `resolveVault()` 模式）
  - 平台级资源 → **拒绝 service token，只接受 session**（照抄 vaults 路由对 `scope=platform` 的处理）
  - 写成功后调 `bumpCatalogVersion(db)`
- **验收**：`route.test.ts` 覆盖 201 / 400 校验失败 / 401 无认证 / 403 service token 被拒 / 500 缺公钥；**并断言响应 JSON 序列化后不含明文子串**
- **依赖**：T1.1, T2.1, T0.2

---
