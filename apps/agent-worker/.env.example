# —— 模型凭据边界（specs/llm-router.md §密钥边界）——
# 本容器**不配置任何供应商密钥**。真 key 只在 llm-router 进程内存里，
# 沙箱拿到的是控制面随 run 签发的短时令牌（ANTHROPIC_AUTH_TOKEN），
# 由 control-worker 通过 /prompt 的 `env` 字段下发，不从这里读。
#
# 因此下面两个变量**必须留空**：dev 模式下配了它们会盖掉注入的网关地址，
# 于是绕过网关直连供应商（docs/plans/llm-router/00-必读.md §五·①）。
# ANTHROPIC_BASE_URL=
# ANTHROPIC_API_KEY=

# 模型名。控制面会随每次 /prompt 传 modelId（= agents.model 解析出的 alias），
# 这里只是没传时的兜底。
CLAUDE_MODEL=sonnet
# ANTHROPIC_MODEL=sonnet

# Server
# PORT=8787
